<div class="editable-field">
    <div class="editable-field__container">
    <label for="{{name}}" class="editable-field__label">{{label}}</label>
        <input 
            type={{type}}
            class="editable-field__input"
            name="{{name}}"
            value="{{value}}"
            disabled={{disabled}}
        >
    </div>
</div>